{
  "name": "gigm_web_automation_cypress",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "cy:run": "cypress run --env allure=true --headed",
    "allure:report": "allure generate allure-results--clean -o allure-report",
    "test": "npm run cy:run || npm install cypress --save-dev || npm run posttest",
    "posttest": "npm run allure:report"
  },
  "author": "Favour Udedibor",
  "license": "ISC",
  "dependencies": {
    "cypress": "^12.8.0"
  },
  "devDependencies": {
    "@cypress/xpath": "^2.0.3",
    "@shelex/cypress-allure-plugin": "^2.37.0",
    "@types/cypress": "^1.1.3",
    "cypress-mochawesome-reporter": "^3.3.0",
    "cypress-xpath": "^2.0.1"
  }
}
